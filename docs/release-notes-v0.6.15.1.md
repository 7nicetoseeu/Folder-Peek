# Folder Peek v0.6.15.1

这次是一个小版本优化，重点放在首层展开手感上。

## 优化

- 优化首层展开手感
- 减少触发阈值附近的视觉干扰，让首层展开更直接、更顺手

## 下载与使用

- `win-x64`
- framework-dependent 与 self-contained 两个版本都会提供
- framework-dependent 版本运行前需要已安装 **.NET 8 Desktop Runtime**

运行后的设置、主题和索引缓存会写入：

`%LocalAppData%\FolderPeek`
